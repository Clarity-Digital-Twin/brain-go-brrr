# Development Environment Variables
# Copy this file to .env for local development

# TUH EEG Dataset Credentials (required for downloading TUH datasets)
# Get credentials from: https://www.isip.piconepress.com/projects/tuh_eeg/
TUH_USERNAME=your_tuh_username
TUH_PASSWORD=your_tuh_password

# Data directory paths
TUH_DATA_DIR=data/datasets/external

# Pre-commit cache location (for local development only)
# This prevents permission issues and speeds up pre-commit hooks
PRE_COMMIT_HOME=./.pre-commit-cache

# Optional: Debug mode for more verbose logging
# BGBR_DEBUG=1

# Optional: Disable YASA for faster testing (speeds up unit tests)
# BGBR_DISABLE_YASA=1

# Optional: Allow synthetic data for testing without real datasets
# BGB_ALLOW_SYNTH_TUAB=1
# BGB_ALLOW_SYNTH_TUEV=1
# BGB_ALLOW_SYNTH_SLEEP_EDF=1
